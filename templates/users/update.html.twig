{% extends 'base.html.twig' %}
{% block title %} Update User {% endblock %}

{% block body %}
    <div class="container mt-3">
        <div class="row">
            <div class="col">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}

                <a href="{{ path('user_list') }}" class="btn btn-primary mt-3 rounded">Cancel</a>
            </div>
            
        </div>
    </div>
{% endblock %}