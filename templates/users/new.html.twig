{% extends 'base.html.twig' %}
{% block title %} New User {% endblock %}

{% block body %}
    <div class="container mt-3">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}

        <a href="{{ path('user_list') }}" class="btn btn-dark rounded mt-3">Cancel</a>
    </div>
{% endblock %}